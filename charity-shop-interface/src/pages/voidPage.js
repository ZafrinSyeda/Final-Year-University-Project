/* Default pages for any page that exists on the menu but will not be implemented for the project */

import React from "react";

const VoidPage = () => {
	return (
		<div>
			<h1>this page does not exist yet</h1>
		</div>
	);
};

export default VoidPage;
